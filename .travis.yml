language: c
jobs:
   include:
      - script: make
      #- script: rm ofile; ./hellomake.exe > ofile
notifications:
  email:
    recipients:
        - chrispthompson@gmail.com
    on_success: always
    on_failure: always
env:
   before_install:
   - export TRAVIS_ROOT=$HOME/travis
   - mkdir -p $TRAVIS_ROOT
   install:
   - make hellomake
   script:
   - ./hellomake
   cache:
     apt: true
     directories:
     - $HOME/travis
